{
  "name": "move.kwan",
  "private": true,
  "scripts": {
    "deploy-kiosk": "cd kiosk-system && npm install && npm run deploy",
    "deploy-todo": "cd todo-list && npm install && npm run deploy",
    "deploy-all": "npm run deploy-kiosk && npm run deploy-todo"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
