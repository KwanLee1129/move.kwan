{
  "scripts": {
    "deploy:kiosk": "gh-pages -d kiosk-system/dist -b gh-pages/kiosk-system",
    "deploy:todo": "gh-pages -d todo-list/dist -b gh-pages/todo-list",
    "deploy": "npm run deploy:kiosk && npm run deploy:todo"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
